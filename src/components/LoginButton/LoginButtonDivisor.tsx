import React from 'react'

interface LoginButtonDivisorProps {
  text: string
}
const LoginButtonDivisor = ({ text }: LoginButtonDivisorProps) => {
  return <span>{text}</span>
}

export default LoginButtonDivisor
